<Window x:Class="TS6_SpeakerOverlay.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:TS6_SpeakerOverlay.Models"
        Title="{DynamicResource Lang_Settings_Title}" Height="620" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <!-- 基础字体 -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#DCDDDE"/>
        </Style>

        <!-- 开关样式 -->
        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Border x:Name="Track" Grid.Column="1" Width="36" Height="20" CornerRadius="10" Background="#72767D" Margin="10,0,0,0">
                                <Ellipse x:Name="Knob" Width="16" Height="16" Fill="White" HorizontalAlignment="Left" Margin="2,0,0,0">
                                    <Ellipse.Effect><DropShadowEffect ShadowDepth="1" BlurRadius="2" Opacity="0.3"/></Ellipse.Effect>
                                </Ellipse>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#4FCD8E"/>
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Knob" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 滑块样式 -->
        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="Foreground" Value="#4FCD8E"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Border Height="4" CornerRadius="2" Background="#40444B" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Background="#4FCD8E" Height="4" BorderThickness="0">
                                        <RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="{TemplateBinding Background}" CornerRadius="2"/></ControlTemplate></RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="16" Height="16" Background="White" VerticalAlignment="Center">
                                        <Thumb.Template><ControlTemplate TargetType="Thumb"><Grid><Ellipse Fill="{TemplateBinding Background}" Stroke="#4FCD8E" StrokeThickness="2"/><Ellipse Fill="#4FCD8E" Width="6" Height="6"/></Grid></ControlTemplate></Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- [核心修复] ComboBox 样式 -->
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#202225"/>
            <Setter Property="BorderBrush" Value="#202225"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- 必须加 ToggleButton 才能点击下拉 -->
                            <ToggleButton x:Name="ToggleButton" 
                                          Grid.Column="2" 
                                          Focusable="false" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid>
                                            <Border Background="{TemplateBinding Background}" BorderBrush="#40444B" BorderThickness="1" CornerRadius="4"/>
                                            <Path Data="M0,0 L4,4 L8,0" Stroke="#AAAAAA" StrokeThickness="1.5" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <!-- 绑定 ToggleButton 背景色 -->
                                <ToggleButton.Background>
                                    <SolidColorBrush Color="#202225"/>
                                </ToggleButton.Background>
                            </ToggleButton>
                            
                            <!-- 内容显示区 (IsHitTestVisible=False 让点击穿透给 ToggleButton) -->
                            <ContentPresenter x:Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Margin="8,0,20,0" 
                                              VerticalAlignment="Center" 
                                              Content="{TemplateBinding SelectionBoxItem}" />
                            
                            <!-- 下拉弹窗 -->
                            <Popup x:Name="Popup" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   Placement="Bottom" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Border Background="#2F3136" BorderThickness="1" BorderBrush="#202225" MaxHeight="150" MinWidth="{TemplateBinding ActualWidth}" CornerRadius="4">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#DCDDDE"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}"><ContentPresenter /></Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="#40444B"/><Setter Property="Foreground" Value="White"/></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="Bd" Property="Background" Value="#4FCD8E"/><Setter Property="Foreground" Value="#202225"/><Setter Property="FontWeight" Value="Bold"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#36393F" CornerRadius="12" BorderThickness="1" BorderBrush="#202225">
        <Border.Effect><DropShadowEffect BlurRadius="20" ShadowDepth="10" Opacity="0.4"/></Border.Effect>

        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="50"/><RowDefinition Height="*"/><RowDefinition Height="60"/></Grid.RowDefinitions>

            <!-- 1. 标题栏 -->
            <Grid Grid.Row="0" Background="#202225" MouseDown="TitleBar_MouseDown">
                <Border CornerRadius="12,12,0,0" Background="Transparent"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <Border Background="#4FCD8E" Width="4" Height="16" CornerRadius="2" Margin="0,0,10,0"/>
                    <TextBlock Text="{DynamicResource Lang_Settings_Title}" FontSize="16" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
                <Button Content="✕" HorizontalAlignment="Right" Click="BtnClose_Click" Foreground="#72767D" Background="Transparent" BorderThickness="0" FontSize="18" FontWeight="Bold" Cursor="Hand" Width="40" Height="40" Margin="0,0,5,0"/>
            </Grid>

            <!-- 2. 内容 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" PanningMode="VerticalOnly" Margin="20,10">
                <StackPanel>
                    <!-- 语言 -->
                    <Border Background="#2F3136" CornerRadius="8" Padding="15" Margin="0,10">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                                <TextBlock Text="LANGUAGE" FontSize="10" FontWeight="Bold" Foreground="#72767D" Margin="0,0,0,2"/>
                                <TextBlock Text="Interface Language" Foreground="White" FontSize="13"/>
                            </StackPanel>
                            <ComboBox DockPanel.Dock="Right" Width="120" VerticalAlignment="Center" SelectedValuePath="Tag" SelectedValue="{Binding Config.Language}" SelectionChanged="LanguageComboBox_SelectionChanged">
                                <ComboBoxItem Content="English" Tag="en-US"/><ComboBoxItem Content="中文 (简体)" Tag="zh-CN"/><ComboBoxItem Content="Français" Tag="fr-FR"/><ComboBoxItem Content="Русский" Tag="ru-RU"/>
                            </ComboBox>
                        </DockPanel>
                    </Border>

                    <!-- 外观 -->
                    <TextBlock Text="{DynamicResource Lang_Appearance}" Foreground="#8E9297" FontSize="11" FontWeight="Bold" Margin="5,10,0,5"/>
                    <Border Background="#2F3136" CornerRadius="8" Padding="15">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,15">
                                <TextBlock Text="{DynamicResource Lang_AvatarMode}" VerticalAlignment="Center"/>
                                <ComboBox DockPanel.Dock="Right" Width="130" SelectedValuePath="Tag" SelectedValue="{Binding Config.AvatarDisplayMode}">
                                    <ComboBoxItem Content="{DynamicResource Lang_Mode_Avatar}"><ComboBoxItem.Tag><models:AvatarMode>Avatar</models:AvatarMode></ComboBoxItem.Tag></ComboBoxItem>
                                    <ComboBoxItem Content="{DynamicResource Lang_Mode_Indicator}"><ComboBoxItem.Tag><models:AvatarMode>Indicator</models:AvatarMode></ComboBoxItem.Tag></ComboBoxItem>
                                    <ComboBoxItem Content="{DynamicResource Lang_Mode_None}"><ComboBoxItem.Tag><models:AvatarMode>None</models:AvatarMode></ComboBoxItem.Tag></ComboBoxItem>
                                </ComboBox>
                            </DockPanel>

                            <StackPanel Margin="0,5">
                                <DockPanel Margin="0,0,0,2"><TextBlock Text="{DynamicResource Lang_FontSize}" FontSize="12"/><TextBlock Text="{Binding Config.FontSize, StringFormat=N0}" DockPanel.Dock="Right" Foreground="#4FCD8E" FontSize="12"/></DockPanel>
                                <Slider Minimum="10" Maximum="30" Value="{Binding Config.FontSize}" Style="{StaticResource ModernSlider}" Margin="0,0,0,15"/>

                                <DockPanel Margin="0,0,0,2"><TextBlock Text="{DynamicResource Lang_AvatarSize}" FontSize="12"/><TextBlock Text="{Binding Config.AvatarSize, StringFormat=N0}" DockPanel.Dock="Right" Foreground="#4FCD8E" FontSize="12"/></DockPanel>
                                <Slider Minimum="0" Maximum="50" Value="{Binding Config.AvatarSize}" Style="{StaticResource ModernSlider}" Margin="0,0,0,15"/>

                                <DockPanel Margin="0,0,0,2"><TextBlock Text="{DynamicResource Lang_Spacing}" FontSize="12"/><TextBlock Text="{Binding Config.ItemSpacing, StringFormat=N0}" DockPanel.Dock="Right" Foreground="#4FCD8E" FontSize="12"/></DockPanel>
                                <Slider Minimum="0" Maximum="20" Value="{Binding Config.ItemSpacing}" Style="{StaticResource ModernSlider}" Margin="0,0,0,15"/>

                                <DockPanel Margin="0,0,0,2"><TextBlock Text="{DynamicResource Lang_Opacity}" FontSize="12"/><TextBlock Text="{Binding Config.BackgroundOpacity, StringFormat=P0}" DockPanel.Dock="Right" Foreground="#4FCD8E" FontSize="12"/></DockPanel>
                                <Slider Minimum="0.0" Maximum="1.0" Value="{Binding Config.BackgroundOpacity}" TickFrequency="0.05" IsSnapToTickEnabled="True" Style="{StaticResource ModernSlider}" Margin="0,0,0,15"/>

                                <DockPanel Margin="0,0,0,2"><TextBlock Text="{DynamicResource Lang_Scale}" FontSize="12"/><TextBlock Text="{Binding Config.UiScale, StringFormat=N1}" DockPanel.Dock="Right" Foreground="#4FCD8E" FontSize="12"/></DockPanel>
                                <Slider Minimum="0.5" Maximum="2.5" Value="{Binding Config.UiScale}" TickFrequency="0.1" IsSnapToTickEnabled="True" Style="{StaticResource ModernSlider}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 行为 -->
                    <TextBlock Text="{DynamicResource Lang_Behavior}" Foreground="#8E9297" FontSize="11" FontWeight="Bold" Margin="5,15,0,5"/>
                    <Border Background="#2F3136" CornerRadius="8" Padding="15">
                        <StackPanel>
                            <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding Config.EnableNotifications}" Margin="0,5">
                                <TextBlock Text="{DynamicResource Lang_EnableNotify}" Foreground="White" Margin="0,0,10,0"/>
                            </CheckBox>
                            <Separator Background="#40444B" Margin="0,10"/>
                            <CheckBox Style="{StaticResource ToggleSwitch}" IsChecked="{Binding Config.ShowOnlyTalking}" Margin="0,5">
                                <TextBlock Text="{DynamicResource Lang_ShowOnlyTalking}" Foreground="White" Margin="0,0,10,0"/>
                            </CheckBox>
                        </StackPanel>
                    </Border>
                    <Canvas Height="20"/>
                </StackPanel>
            </ScrollViewer>

            <!-- 3. 底部按钮 -->
            <Border Grid.Row="2" Background="#292B2F" CornerRadius="0,0,12,12" Padding="20,0">
                <Grid>
                    <Button Content="{DynamicResource Lang_Reset}" Click="BtnReset_Click" Foreground="#ED4245" Background="Transparent" BorderThickness="0" Cursor="Hand" FontSize="12" HorizontalAlignment="Left"/>
                    <Button Content="{DynamicResource Lang_Save}" Click="BtnSave_Click" Background="#4FCD8E" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Height="32" Width="100" HorizontalAlignment="Right">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                        <Button.Effect><DropShadowEffect Color="#4FCD8E" BlurRadius="10" Opacity="0.4" ShadowDepth="2"/></Button.Effect>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>